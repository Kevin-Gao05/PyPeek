# 06 — 轻量 Onboarding

**What to build:** 首次扫描完成后显示 3 步高亮指引，教新用户关键交互。非首次启动不显示。

**Blocked by:** 01（软依赖）— 建议先做 01，用 `load_cache()` 返回 `None` 判断首次用户。若独立实现，可临时用 `os.path.exists(cache_path)` 替代。

**Status:** ready-for-agent

- [ ] 首次扫描完成（`scan_complete` 事件）后，显示 onboarding 指引（非首次启动跳过）
- [ ] 第 1 步：高亮任意 Python/venv 表格行，提示「点击行可查看已安装的包」。按钮：「下一步」「跳过」
- [ ] 第 2 步：高亮安全徽章区域，提示「安全 / 警告 / 危险 — 卸载前看清等级」。按钮：「上一步」「下一步」「跳过」
- [ ] 第 3 步：高亮统计卡片，提示「点击卡片可快速跳转到对应标签页」。按钮：「上一步」「完成」
- [ ] 高亮效果用半透明遮罩 + 目标元素 z-index 提升（纯 CSS，不引入 tour 库）
- [ ] 「跳过」和「完成」均关闭 onboarding，不再出现
- [ ] Onboarding 不会因关闭窗口而重置——只要缓存存在就不显示
